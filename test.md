# Design: LEVERAGE — Vendor Negotiation Intelligence Agent (Hackathon, 10 hr)

Generated by /office-hours on 2026-04-18
Branch: main
Repo: garrytan-gstack (design authored here; target project LEVERAGE is a separate repo)
Status: DRAFT
Mode: Builder
Event: CascadeFlow + Hindsight Hackathon
Promo code: MEMHACK409 ($50 Hindsight Cloud credits)

## Problem Statement

Procurement managers at mid-market and enterprise companies negotiate the same vendor renewals year after year, but institutional memory about what tactics worked — and when — is fragmented across Slack threads, call notes, email archives, and the heads of people who left the company. They walk into every renewal with a generic playbook and forget what moved the needle last time. LEVERAGE is a pre-call briefing tool that turns Hindsight Cloud memory into scored, evidence-backed negotiation tactics and updates those scores live as new calls are logged.

For the hackathon, the deliverable is a working demo of the procurement wedge: one dashboard, three seeded vendors (NexaCloud, DataPipe, SecureNet), a briefing screen with a before/after memory toggle as the hero moment, and a post-call logger that visibly updates tactic confidence scores.

## What Makes This Cool

- **Memory IS the product.** Without Hindsight, the app is a ChatGPT wrapper. With it, each vendor accumulates a private, compounding knowledge base that gets sharper every call. The demo makes this tangible in one toggle click.
- **Evidence-scored tactics.** Every recommendation comes with a confidence score (0.0–1.0), a track record (e.g. "2 for 2"), timing guidance, and the actual history behind it. Feels less like an LLM guess, more like a coach who's watched every past call.
- **Live score evolution.** After logging a call, confidence scores diff in real time. A successful tactic ticks up. A failed one ticks down. The memory visibly learns in front of the judge.
- **Hackathon-to-company arc.** The narrative expands cleanly from "vendor renewals" to "every repeated B2B negotiation" — sales close, legal settlements, M&A, hiring packages. Judges and investors see a feature and a company in the same demo.

## Constraints

- **Hard time budget: 10 hours end-to-end.** Spec as originally written was 13–15 hrs of work. We cut to fit.
- **Required sponsor tools:** Hindsight Cloud (memory), CascadeFlow (orchestration), OpenAI GPT-4 class model (synthesis). CascadeFlow must be visibly used in the demo, not buried in backend plumbing.
- **Minimum two rehearsals** of the full 5-minute demo before the submission cutoff. Backup recording must exist by hour 8.
- **Public GitHub repo** with clean README, accurate `.env.example`, no secrets committed.
- **Seeder must run at least 24 hours before demo.** Hindsight needs time for Observation synthesis to stabilize. Empty memory banks = disqualifying.
- **No real integrations.** No OAuth, no live CRM/email ingestion, no billing, no mobile responsive, no multi-user auth.

## Premises

All five premises below were presented to the builder and agreed to. They shape every downstream decision.

1. **Memory IS the product.** Without Hindsight the app has nothing to show. The architecture's job is to make memory visible, scored, and live.
2. **The hero demo moment is the before/after memory toggle** (generic AI advice vs. scored tactics with evidence), not the post-call diff animation. This flips P0/P1 from the original spec: toggle → P0 hero, post-call diff → P0 supporting beat with simpler CSS-transition animation.
3. **10 hours requires pre-committed scope cuts.** Temporal banner inlines to a one-line badge on the dashboard card. Recent signals feed trims to 2 items. Diff animation is CSS `transition` + arrow icon, not an animation library. Confidence-evolution chart and email-parsing P1 features stay P1 — will not ship.
4. **"Million dollar idea" is narrative framing, not scope expansion.** Same code, different pitch. The README hero, demo intro, and closing slide position LEVERAGE as "institutional memory for every repeated B2B negotiation" with procurement as wedge, sales/legal/M&A/hiring as expansion.
5. **CascadeFlow must be visible in the demo.** Proposal: during briefing load and during ingest, render a 3-line status trail showing CascadeFlow orchestrating the pipeline (recall → synthesize → rank). Non-trivial visually, 20 min of dev, earns sponsor judge points.

## Approaches Considered

### Approach A: Narrow deep — NexaCloud only, one screen

- Skip the dashboard. Demo opens directly on the NexaCloud briefing.
- All polish goes into one screen: toggle + tactics + signals + inline post-call form.
- Pros: Less to build, less to break, every pixel rehearsed. Highest polish per hour.
- Cons: Loses the "this scales to 1000s of vendors" story. Dashboard is a classic platform signal — dropping it weakens the company framing.
- Effort: 7 hr dev / 2 hr polish / 1 hr buffer. Completeness 8/10.

### Approach B: Broad shallow — spec with the flips _(chosen)_

- 3-vendor dashboard with lightweight cards.
- Briefing screen with toggle as hero, tactics section, trimmed 2-item signals feed, inline temporal badge.
- Post-call form with CSS-transition diffs as supporting beat.
- CascadeFlow status trail on briefing load and ingest.
- Pros: Tells the scale story, strongest "million dollar" feel, closest to spec already designed.
- Cons: Tightest timeline if Hindsight integration surprises land in hour 4–5.
- Effort: 9 hr dev / 45 min polish / 15 min buffer. Completeness 9/10.

### Approach C: Chat-first — no dashboard

- LEVERAGE is a chat interface. User types "call with NexaCloud in 4 hours" → briefing inline → "here's what happened on the call" → memory updates inline.
- Same Hindsight calls; no dashboard, no routing, one screen.
- Pros: 2026-native UI. Feels like an AI product, not a CRM. Judges respond to chat demos.
- Cons: Requires explaining "why chat" to judges (procurement managers don't actually want chat, they want pre-call briefings). The narrative tax eats demo time.
- Effort: 6 hr dev / 2 hr polish / 2 hr buffer. Completeness 7/10.

## Recommended Approach

**Approach B: Broad shallow with all premise flips applied.**

Architecture:

```
leverage/
├── seeder/
│   ├── seed_vendors.py
│   └── vendor_data.py          # 53 interactions
├── backend/
│   ├── main.py                 # FastAPI routes + CORS
│   ├── briefing.py             # reflect() → tactics JSON
│   ├── ingest.py               # §5.4 flow: old scores → retain → new scores → diffs
│   ├── nomemory.py             # Groq-only generic-advice endpoint (powers toggle)
│   ├── cascade.py              # CascadeFlow orchestration wrapping reflect/retain
│   ├── models.py               # Pydantic schemas
│   └── prompts.py              # BRIEFING_PROMPT + NOMEMORY_PROMPT
├── frontend/                   # Next.js 15 (App Router) + Tailwind v3.4
│   └── src/
│       ├── app/
│       │   ├── page.tsx        # Dashboard
│       │   └── briefing/[vendor]/page.tsx
│       ├── lib/api.ts
│       └── components/
│           ├── VendorCard.tsx
│           ├── TacticCard.tsx
│           ├── MemoryToggle.tsx      # hero component
│           ├── CascadeStatus.tsx     # orchestration status trail
│           ├── SignalFeed.tsx
│           ├── PostCallForm.tsx
│           └── ScoreDiff.tsx
├── .env.example
├── requirements.txt
├── package.json
└── README.md
```

Tech stack corrections from the original spec:

| Original spec       | Corrected                           |
| ------------------- | ----------------------------------- |
| Next16              | Next.js 15 (App Router)             |
| Tailwins v1.6       | Tailwind v3.4                       |
| gpt-5-4             | gpt-4o (or gpt-5 if key has access) |
| React `.jsx` folder | `.tsx` under Next `app/`            |
| EC2 backend host    | Railway (matches §8 step 11)        |

The hero moment — `MemoryToggle.tsx` — is a single switch at the top of the briefing screen. OFF calls `POST /api/briefing/nomemory` (Groq with only the vendor name, no context) and renders generic textbook advice ("consider mentioning competitors," "ask for a discount"). ON calls `GET /api/briefing?vendor=` and renders scored tactics with evidence, timing, and track records. The contrast sells Hindsight in three seconds.

The supporting beat — `PostCallForm` + `ScoreDiff` — executes the exact 4-step flow in spec §5.4 (capture old scores → retain() → recompute → diff with 0.01 threshold). Diffs render as `0.87 → 0.91` with CSS `transition: color 400ms` and ↑/↓ arrow. Green on up, red on down. No animation library.

`CascadeStatus.tsx` renders on both briefing load and ingest:

```
✓ Recalled 23 memories from Hindsight (240ms)
✓ Synthesized 4 tactics via gpt-4o (1.2s)
✓ Ranked by confidence (10ms)
```

Fades out after tactics render. Makes CascadeFlow the nervous system of the demo rather than an invisible dependency.

### Build order — 10-hour sequence with pre-committed cut checkpoints

| Hour     | Work                                                                                                                                                                                                 | Cut rule if behind                                                                                                               |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 0.0–1.0  | Hindsight account + MEMHACK409 promo + `.env` scaffold + trivial `retain()` test. Next app scaffold + Tailwind. Repo on GitHub with `.env.example`.                                                  | If Hindsight signup delays >30 min, open a support ticket and proceed with mocked client locally.                                |
| 1.0–3.5  | `vendor_data.py` — 53 interactions. Real names, dollar amounts, dates. Faker for filler only. Embed every pattern in §4.3 with at least as many datapoints as `total_uses`. **Longest single task.** | At hour 3.5: if not done, ship with 40 interactions (drops NexaCloud 23→18, DataPipe 16→12, SecureNet 14→10).                    |
| 3.5–5.0  | `main.py` routes, `briefing.py` with `BRIEFING_PROMPT`, `nomemory.py` with generic advice prompt. Curl-test both.                                                                                    | At hour 5: if `reflect()` returns invalid JSON, fallback is a Groq call wrapped in a try/except with the same prompt.            |
| 5.0–6.0  | `ingest.py` §5.4 flow. Curl-test with sample note. Confirm diffs render.                                                                                                                             | At hour 6: if ingest broken, stub a deterministic `/api/ingest` that returns canned diffs. The demo works without live retain(). |
| 6.0–7.0  | `VendorCard`, Dashboard page, `api.ts` client, basic routing.                                                                                                                                        | At hour 7: if dashboard unstyled, ship with minimal Tailwind — judges look at the briefing screen, not this.                     |
| 7.0–8.5  | Briefing page: `TacticCard`, `MemoryToggle` (hero), `CascadeStatus`, trimmed signals, inline temporal badge.                                                                                         | At hour 8.5: if toggle doesn't render, drop it and revert to the original diff-as-hero (weaker but shippable).                   |
| 8.5–9.0  | `PostCallForm` + `ScoreDiff`. Deploy to Vercel + Railway. Fix CORS.                                                                                                                                  | At hour 9: if deploy fails, demo locally and record backup video immediately.                                                    |
| 9.0–9.5  | Smoke-test end-to-end 3×. Record backup demo video (even if deploy succeeded — always have this).                                                                                                    | —                                                                                                                                |
| 9.5–10.0 | Rehearse live demo 2×. Final README polish.                                                                                                                                                          | —                                                                                                                                |

### The 5-minute demo script

**Beat 1 (0:00–0:30) — Cold open: meet Priya.** One slide or narration line: "Priya is a procurement manager at a 2,000-person SaaS company. She renews NexaCloud in 4 hours — a $380k contract. Her company has negotiated with NexaCloud three times before. She remembers almost none of it."

**Beat 2 (0:30–1:00) — Dashboard.** Three vendor cards. Red badge on NexaCloud: "18 days · fiscal Q3 pressure." Click in.

**Beat 3 (1:00–2:30) — The toggle (hero).** Show the briefing screen with memory OFF. Generic AI advice: "Consider competitive bids. Ask about discounts." Flat, useless. Flip the toggle ON. CascadeStatus trail flashes through. Four scored tactics render: _Mention AWS (0.87, 2/2 success, last worked May 2024 — 3% discount + $8k support tier). Fiscal Q3 pressure (0.74, 3/4). Multi-year commitment (0.50, raised unprompted Nov 2024). Cancellation threat (0.12, failed in 2022 — avoid)._ This is the "whoa."

**Beat 4 (2:30–3:30) — Post-call log.** Priya comes back from the call. Logs: "Marcus pushed back harder than expected when I mentioned AWS. Offered a 3-year commitment unprompted — 8% discount plus roadmap access." Submit. CascadeStatus runs. AWS mention ticks down 0.87 → 0.79. Multi-year ticks up 0.50 → 0.68. Green and red arrows. Memory learned.

**Beat 5 (3:30–4:30) — The dollar number.** Summary slide: "Priya's company saved $23,400 on this single renewal. Across 47 active vendor contracts, this is $1.1M/year in unlocked value per procurement team. LEVERAGE pays for itself in one call."

**Beat 6 (4:30–5:00) — The company frame.** Closing slide: "Procurement is the wedge. Sales close, legal settlements, M&A term sheets, hiring packages, partnership deals — every repeated B2B negotiation has the same memory problem. LEVERAGE is the memory layer. Thank you."

## Open Questions

- **What LLM does the user's OpenAI key support?** Spec said "gpt-5-4" which doesn't exist. If the key has GPT-5 access, use it for the briefing prompt (better JSON fidelity). If only GPT-4, use `gpt-4o` — it's reliable with structured output.
- **Is Groq actually needed, or is it a fallback only?** Spec lists `GROQ_API_KEY` but the flow only calls OpenAI. Recommend: use Groq for the `nomemory` endpoint (generic advice) because it's fast and free-tier friendly — good contrast with OpenAI-backed Hindsight briefing.
- **CascadeFlow specifics.** Which CascadeFlow primitives are required (steps, pipelines, sub-agents)? The status trail UI works regardless, but backend implementation depends on CascadeFlow's API surface. Read sponsor docs hour 0.
- **Does Hindsight's `reflect()` return stable JSON?** Risk in §10 of spec. Mitigation: wrap `json.loads()` in try/except; on parse failure, call OpenAI directly with the same prompt. Pre-test this hour 3.5.
- **Demo venue constraints.** Is the live demo on stage or over video call? Over video, run everything locally. On stage, always have the backup recording queued — spec §10 mitigation is correct.

## Success Criteria

All from spec §9, plus three additions covering the new hero moment and "million dollar" framing:

- `python seeder/seed_vendors.py` completes without error and populates 3 Hindsight banks.
- `GET /api/vendors` returns 3 vendors with correct stats.
- `GET /api/briefing?vendor=nexacloud` returns parseable JSON matching §5.2 schema, ≥3 tactics descending by confidence, top tactic ≥ 0.80.
- Dashboard renders 3 vendor cards; NexaCloud card shows red "days remaining" badge.
- Briefing screen renders within 2 seconds.
- Tactic cards show correct dot colours per §6.2 thresholds.
- **NEW: Memory toggle OFF renders generic advice in <2s. Toggle ON renders scored tactics in <3s. Toggle flip transitions visibly within 500ms.**
- **NEW: CascadeFlow status trail renders on briefing load and ingest, showing 3 visible steps.**
- Post-call form submission triggers the full §5.4 flow.
- Score diffs render `old → new` with CSS transition + arrow in green/red within 3 seconds of submit.
- At least one tactic's confidence visibly changes per post-call log.
- Full demo runs in ≤5 minutes with no dead time.
- **NEW: README includes the "million dollar" framing paragraph in the hero section, not buried.**
- Repo is public, README renders, `.env.example` present, no secrets committed.

## Distribution Plan

Hackathon submission, no user distribution required. Deployment targets:

- **Frontend:** Vercel free tier (auto-deploy from `main`).
- **Backend:** Railway (matches §8). Python + FastAPI buildpack. One environment variable group.
- **Demo repo:** Public GitHub, clean commit history, README explains the product in the first paragraph (Priya opener, not tech stack).
- **Backup plan:** Recorded 5-min demo video hosted as a GitHub Release asset. Linked in README. Always queued and ready to swap in live if anything fails on stage.
- **Post-hackathon:** If judges or investors engage, the expansion pitch (sales → legal → M&A → hiring) is in the README and the closing slide. No landing page needed for the submission window.

## Next Steps

1. Create `leverage` repo on GitHub. Scaffold Next.js 15 frontend + Python FastAPI backend + seeder folder per structure above.
2. Sign up for Hindsight Cloud, apply MEMHACK409 promo, verify API key works with a trivial `client.retain()` + `client.reflect()` round-trip.
3. Start `vendor_data.py`. Allot 2.5 hrs. Get the NexaCloud 23 interactions right — they're the demo centerpiece. DataPipe and SecureNet can use more Faker filler.
4. Build backend in this order: `briefing.py` → `nomemory.py` → `ingest.py`. Curl-test each before touching frontend.
5. Build frontend in this order: `api.ts` → Dashboard → Briefing with toggle + CascadeStatus → PostCallForm + ScoreDiff.
6. Deploy at hour 8. Record backup video at hour 8. Rehearse at hour 9.
7. At hour 10: submit, write the 400–600 word article, draft the LinkedIn post.

## What I noticed about how you think

- You brought a fully specified plan, including API schemas, folder structure, prompt strings, and a 9-step build order. That's rare. Most people show up with an idea; you showed up with a spec. That ceiling is high — you're going to ship regardless of what I say.
- The phrases that caught my ear: _"Memory is the product — without Hindsight the app has nothing to show."_ That's founder-grade framing. You already located the moat before the code exists. Most hackathon teams discover that concept on stage while someone else is demoing.
- You named the demo-winning moment explicitly ("post-call log submit → animated confidence score diffs. This must be flawless. Everything else is secondary."). That instinct — to name one moment and protect it from scope creep — is exactly what wins hackathons. I pushed back on _which_ moment, not on the instinct. The instinct is right.
- The things that concern me: the tech stack typos and version contradictions tell me the spec was drafted in a rush or by someone who didn't sanity-check against current stable versions. For a hackathon that's forgivable; for a YC application, it's a tell. If this becomes a company, tighten the artifact.
- You wrote "million dollar idea" in your request but the spec itself is disciplined and narrow. That contrast is healthy — it means you know the difference between the pitch and the build. Keep the pitch big and the build small. That's how you win both the hackathon and the conversation that follows it.
